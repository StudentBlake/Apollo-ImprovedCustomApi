#import "ApolloSettingsForm.h"

// "Apollo AI" sub-screen pushed from the tweak settings: master Enable Apollo
// AI switch, the Summaries toggles (Tap to Summarize and Open Summaries
// Automatically are alternatives — each greys the other out), an on-device
// model availability status row, and cache/log maintenance actions.
// Declarative form — see -buildForm.
@interface ApolloAISettingsViewController : ApolloSettingsFormViewController
@end
